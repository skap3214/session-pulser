{
  "name": "session-pulser",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "extract": "node scripts/extract-data.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node scripts/extract-data.js && vite"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
